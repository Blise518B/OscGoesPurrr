# Test package for the standalone toy simulator.
#
# Kept under toysim/ (not the main app's top-level tests/) so OscGoesPurrr's
# `pytest` run — scoped by pytest.ini's `testpaths = tests` — never collects
# it. Run these on their own from the repo root with:  pytest toysim
