# Development-only dependencies — not shipped with the app, not required
# at runtime. Install on top of `requirements.txt` when contributing:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# SPDX-License-Identifier: MIT

pytest>=7.4
