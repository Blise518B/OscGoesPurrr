# OscGoesPurrr runtime dependencies. Adding or removing one? Regenerate
# THIRD_PARTY_NOTICES.md with tools/make_third_party_notices.py.
#
# SPDX-License-Identifier: MIT

PySide6>=6.5.0
buttplug>=1.0.0
python-osc>=1.8.0
# Zeroconf/mDNS — required for VRChat OSCQuery auto-discovery
# (_osc._udp.local advertisement + ServiceBrowser). Imported unguarded by
# vrchat_osc.py, so the app cannot start without it.
zeroconf>=0.131.0
# Tray icon: pystray draws it, Pillow renders the glow frames.
pystray>=0.19.0
Pillow>=9.0.0
# Used by vrchat_osc.py for the OSCQuery HTTP probe and by update_checker.py
# for the GitHub release check. The update check's import is guarded — a
# missing requests only disables updating, it never blocks startup.
requests>=2.31
